# ─── Application ──────────────────────────────────────────
NODE_ENV=development
PORT=3000
APP_URL=https://worktrips-doc.azurewebsites.net
FRONTEND_URL=https://worktrips-doc.azurewebsites.net

# ─── PostgreSQL (Azure Database for PostgreSQL Flexible Server) ──
DB_HOST=worktrips-doc-db.postgres.database.azure.com
DB_PORT=5432
DB_NAME=worktrips_doc
DB_USER=wtdoc_admin
DB_PASSWORD=your_strong_password_here
DB_SSL=true
DB_POOL_MAX=20
DB_POOL_IDLE_TIMEOUT=30000
DB_POOL_CONNECTION_TIMEOUT=10000

# ─── JWT ──────────────────────────────────────────────────
JWT_SECRET=your_very_long_random_jwt_secret_min_64_chars
JWT_EXPIRES_IN=8h
JWT_REFRESH_EXPIRES_IN=7d

# ─── SAML / GCP Workspace SSO ─────────────────────────────
SAML_ENTRY_POINT=https://accounts.google.com/o/saml2/idp?idpid=XXXXXXXXXX
SAML_ISSUER=worktrips-doc
SAML_CALLBACK_URL=https://worktrips-doc.azurewebsites.net/api/auth/saml/callback
# Base64-encoded X.509 cert from GCP Workspace SAML IdP metadata XML
SAML_IDP_CERT=MIID...base64encodedcert...

# ─── Google OAuth2 / Gmail ────────────────────────────────
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your_secret
GOOGLE_REDIRECT_URI=https://worktrips-doc.azurewebsites.net/api/crm/gmail/oauth/callback

# Pub/Sub topic — wymagany do rejestracji Gmail watch (gmail.users.watch)
# format: projects/<project-id>/topics/<topic-name>
GOOGLE_PUBSUB_TOPIC=projects/worktrips-crm/topics/gmail-push-notifications

# Pub/Sub subskrypcja PULL — aplikacja sama odpytuje kolejkę (nie wymaga publicznego endpointu)
# format: projects/<project-id>/subscriptions/<subscription-name>
GOOGLE_PUBSUB_SUBSCRIPTION=projects/worktrips-crm/subscriptions/gmail-pull

# Interwał pollingu (ms), domyślnie 30000 (30 sekund)
# PUBSUB_POLL_INTERVAL_MS=30000

# Service account do autoryzacji Pub/Sub pull (jedno z poniższych):
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key":"..."}
# GOOGLE_SERVICE_ACCOUNT_FILE=/path/to/service-account.json
# (lub ustaw GOOGLE_APPLICATION_CREDENTIALS dla ADC)

# ─── Azure Blob Storage ───────────────────────────────────
AZURE_STORAGE_ACCOUNT_NAME=worktrips doc
AZURE_STORAGE_ACCOUNT_KEY=your_storage_account_key
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=...
AZURE_STORAGE_CONTAINER=documents

# ─── Azure Key Vault ──────────────────────────────────────
AZURE_KEYVAULT_URL=https://worktrips-doc-kv.vault.azure.net/
AZURE_CLIENT_ID=your_service_principal_client_id
AZURE_CLIENT_SECRET=your_service_principal_secret
AZURE_TENANT_ID=your_azure_tenant_id

# ─── Azure Application Insights ───────────────────────────
APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=...

# ─── Email / Azure Communication Services ────────────────
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=apikey
SMTP_PASS=your_sendgrid_api_key
EMAIL_FROM=noreply@worktrips.com
EMAIL_FROM_NAME=worktrips.doc

# ─── Signus API ───────────────────────────────────────────
SIGNUS_API_BASE_URL=https://api.signus.eu/v1
SIGNUS_API_KEY=your_signus_api_key
SIGNUS_WEBHOOK_SECRET=your_signus_webhook_secret

# ─── Rate Limiting ────────────────────────────────────────
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=200

# ─── File Upload ──────────────────────────────────────────
MAX_FILE_SIZE_MB=50
ALLOWED_MIME_TYPES=application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document,application/msword
